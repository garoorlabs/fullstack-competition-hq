package com.leaguehq.dto.request;

import lombok.Data;

@Data
public class CreateConnectAccountRequest {
    // Empty for now - user ID comes from JWT
    // Could add returnUrl, refreshUrl if needed
}
